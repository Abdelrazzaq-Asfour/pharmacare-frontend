// // Component Registry - Architectural metadata for atomic UI components
export const COMPONENT_REGISTRY = {
  common: [
    "Button.jsx (Optimized button states with loading spinners)",
    "Input.jsx (Sanitized input fields with error boundary binding)",
    "Modal.jsx (Accessible dialog containers with focus trapping)"
  ],
  tables: [
    "DataTable.jsx (Virtualized high-performance table for massive batch data volumes)"
  ]
};